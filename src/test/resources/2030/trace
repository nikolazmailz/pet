@Value("${audit.kafka.bootstrap-servers}")
private String bootstrapServers;

@Value("${audit.kafka.topic}")
private String auditTopic;

@Test
void printAuditConfig() {
    System.out.println(bootstrapServers);
    System.out.println(auditTopic);
}
