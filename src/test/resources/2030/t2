@Configuration
public class SrhrAuditConfig {

    @Bean
    @Order(Ordered.LOWEST_PRECEDENCE - 1000)
    public SrhrAuditResolver srhrAuditResolver() {
        return new SrhrAuditResolver();
    }
}
