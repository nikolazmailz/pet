package ru.ntdev.srhr.ms.salarychange.client;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.http.converter.json.MappingJackson2HttpMessageConverter;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.salarychange.config.JacksonConfig;
import ru.ntdev.srhr.ms.salarychange.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {

    private final JacksonConfig jacksonConfig;

    @Autowired
    public EasupIntegrationClient(JacksonConfig jacksonConfig) {
        this.jacksonConfig = jacksonConfig;
    }

    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, headerRoles, realmHeader));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        ObjectMapper customObjectMapper = jacksonConfig.objectMapper(); // Step 1
        MappingJackson2HttpMessageConverter customConverter = jacksonConfig.createCustomConverter(customObjectMapper); // Step 2

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .messageConverters(customConverter)
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);
        return restTemplate;
    }
}

package ru.ntdev.srhr.ms.teamcalendar.client;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.http.converter.json.MappingJackson2HttpMessageConverter;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.teamcalendar.config.JacksonConfig;
import ru.ntdev.srhr.ms.teamcalendar.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {

    private final JacksonConfig jacksonConfig;

    @Autowired
    public EasupIntegrationClient(JacksonConfig jacksonConfig) {
        this.jacksonConfig = jacksonConfig;
    }

    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, realmHeader, headerRoles));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        ObjectMapper customObjectMapper = jacksonConfig.objectMapper(); // Step 1
        MappingJackson2HttpMessageConverter customConverter = jacksonConfig.createCustomConverter(customObjectMapper); // Step 2

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .messageConverters(customConverter)
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);
        return restTemplate;
    }
}


package ru.ntdev.srhr.ms.timesheet.client;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.http.converter.json.MappingJackson2HttpMessageConverter;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.timesheet.config.JacksonConfig;
import ru.ntdev.srhr.ms.timesheet.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {
    @Autowired final JacksonConfig jacksonConfig;

    public EasupIntegrationClient(JacksonConfig jacksonConfig) {
        this.jacksonConfig = jacksonConfig;
    }

    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, realmHeader, headerRoles));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        ObjectMapper customObjectMapper = jacksonConfig.objectMapper(); // Step 1
        MappingJackson2HttpMessageConverter customConverter = jacksonConfig.createCustomConverter(customObjectMapper); // Step 2

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .messageConverters(customConverter)
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);

        return restTemplate;
    }
}


package ru.ntdev.srhr.ms.userphoto.client;

import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.userphoto.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {

    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, realmHeader, headerRoles));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);
        return restTemplate;
    }
}


package ru.ntdev.srhr.ms.vacationreq.client;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.http.converter.json.MappingJackson2HttpMessageConverter;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.vacationreq.config.JacksonConfig;
import ru.ntdev.srhr.ms.vacationreq.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {

    private final JacksonConfig jacksonConfig;

    @Autowired
    public EasupIntegrationClient(JacksonConfig jacksonConfig) {
        this.jacksonConfig = jacksonConfig;
    }

    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, realmHeader, headerRoles));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        ObjectMapper customObjectMapper = jacksonConfig.objectMapper(); // Step 1
        MappingJackson2HttpMessageConverter customConverter = jacksonConfig.createCustomConverter(customObjectMapper); // Step 2

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .messageConverters(customConverter)
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);
        return restTemplate;
    }
}


package ru.ntdev.srhr.ms.works.schedule.client;

import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.works.schedule.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {

    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, realmHeader, headerRoles));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);
        return restTemplate;
    }
}


package ru.ntdev.srhr.ms.yearbonus.client;


import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.yearbonus.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {

    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, realmHeader, headerRoles));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);
        return restTemplate;
    }
}

package ru.ntdev.srhr.ms.payavg.client;

import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.payavg.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {

    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, realmHeader, headerRoles));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);
        return restTemplate;
    }
}


package ru.ntdev.srhr.ms.paystub.client;

import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.paystub.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {

    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, realmHeader, headerRoles));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);
        return restTemplate;
    }
}


package ru.ntdev.srhr.ms.scheduleHoliday.client;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.http.converter.json.MappingJackson2HttpMessageConverter;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.scheduleHoliday.config.JacksonConfig;
import ru.ntdev.srhr.ms.scheduleHoliday.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {

    private final JacksonConfig jacksonConfig;

    @Autowired
    public EasupIntegrationClient(JacksonConfig jacksonConfig) {
        this.jacksonConfig = jacksonConfig;
    }


    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, realmHeader, headerRoles));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        ObjectMapper customObjectMapper = jacksonConfig.objectMapper(); // Step 1
        MappingJackson2HttpMessageConverter customConverter = jacksonConfig.createCustomConverter(customObjectMapper); // Step 2

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .messageConverters(customConverter)
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);
        return restTemplate;
    }
}


package ru.ntdev.srhr.ms.staff.info.client;

import org.apache.hc.client5.http.classic.HttpClient;
import org.apache.hc.client5.http.impl.classic.HttpClients;
import org.apache.hc.core5.http.Header;
import org.apache.hc.core5.http.HttpHeaders;
import org.apache.hc.core5.http.message.BasicHeader;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.http.client.ClientHttpRequestInterceptor;
import org.springframework.http.client.HttpComponentsClientHttpRequestFactory;
import org.springframework.stereotype.Component;
import org.springframework.util.CollectionUtils;
import org.springframework.web.client.RestTemplate;
import ru.ntdev.srhr.common.dto.api.SapSystemParamsDto;
import ru.ntdev.srhr.ms.staff.info.filter.IntegrationRequestInterceptor;

import java.util.*;


@Component
public class EasupIntegrationClient {

    public RestTemplate buildRestTemplate(SapSystemParamsDto systemParams) {

        Header headerPernr = new BasicHeader("pernr", systemParams.getTabNumber());
        Header headerSessionId = new BasicHeader("sessionId", systemParams.getSessionId());
        Header headerRole = new BasicHeader("role", systemParams.getRole());
        Header headerLogin = new BasicHeader("X-user-login", systemParams.getAdLogin());
        Header headerRoles = new BasicHeader("Roles", systemParams.getRolesHeader());
        Header traceHeader = new BasicHeader("logId", systemParams.getTraceId());
        Header realmHeader = new BasicHeader("realm", systemParams.getRealm());
        List<Header> headers = new ArrayList<>(Arrays.asList(headerPernr, headerSessionId, headerRole, headerLogin, traceHeader, realmHeader, headerRoles));


        HttpClient client = HttpClients
                .custom()
                .setDefaultHeaders(headers)
                .build();

        Map<String,String> headersValue = new HashMap<>(headers.size());
        for (Header header : headers ) {
            headersValue.put(header.getName(),header.getValue());
        }

        RestTemplate restTemplate = new RestTemplateBuilder()
                .defaultHeader(HttpHeaders.CONTENT_TYPE,"application/json;charset=UTF-8")
                .defaultHeader("pernr", headersValue.get("pernr"))
                .defaultHeader("sessionId", headersValue.get("sessionId"))
                .defaultHeader("logId", headersValue.get("logId"))
                .build();

        restTemplate.setRequestFactory(new HttpComponentsClientHttpRequestFactory(client));
        List<ClientHttpRequestInterceptor> interceptors = restTemplate.getInterceptors();
        if (CollectionUtils.isEmpty(interceptors)) {
            interceptors = new ArrayList<>();
        }
        interceptors.add(new IntegrationRequestInterceptor());
        restTemplate.setInterceptors(interceptors);
        return restTemplate;
    }
}
