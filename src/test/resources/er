   @Query(value = "select * from srhr_requisition.vacancy p where p.vacancy_id =  :vacancyId ", nativeQuery = true)
    VacancyEntity findVacancyStatusEntityByVacancyId(
            @Param("vacancyId") String vacancyId
    );



              - column:
                  name: guid
                  type: varchar(32)
                  constraints:
                    unique: true
                    nullable: false
