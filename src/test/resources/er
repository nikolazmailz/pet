 @Autowired
    EntityManager em;

    @Autowired
    TransactionTemplate transactionTemplate;     // ← Spring сам инжектит, бин есть всегда

    @BeforeEach
    void cleanDb() {
        transactionTemplate.executeWithoutResult(status ->
            em.createNativeQuery("DELETE FROM task_blocks").executeUpdate()
        );
    }
